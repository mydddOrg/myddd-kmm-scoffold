pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "myddd-kmm-scoffold"

include(":androidApp")
include(":shared")

